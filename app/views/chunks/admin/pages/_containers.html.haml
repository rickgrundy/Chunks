-@page.containers.each do |container|
  .section.container
    %h3=container.title
    -container.chunks.each do |chunk|
      =render partial: "chunk", locals: {f: f, chunk: chunk}
    
    Add a chunk: 
    -container.available_chunks.each do |chunk_class|
      =link_to chunk_class.title, chunks_admin_chunks_path(:page_id => @page.id, container_key: container.key, type: chunk_class), method: "post1"
      |